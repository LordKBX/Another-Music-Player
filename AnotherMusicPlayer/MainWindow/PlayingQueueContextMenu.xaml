<ContextMenu x:Class="AnotherMusicPlayer.PlayingQueueContextMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">

    <!-- Playlist Global ContextMenu -->
    <MenuItem Name="ClearPlaylist" Header="{DynamicResource PlayingQueueCMClrear}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="PlaylistRemove"/>
        </MenuItem.Icon>
    </MenuItem>

    <!-- Playlist Item ContextMenu -->
    <MenuItem Name="RemoveTracks" Header="{DynamicResource PlayingQueueCMRemove}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="CloseThick"/>
        </MenuItem.Icon>
    </MenuItem>
</ContextMenu>
