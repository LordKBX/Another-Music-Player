<Button x:Class="AnotherMusicPlayer.LibraryFolderButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnotherMusicPlayer"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="90" d:DesignWidth="90">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="40px"/>
            <RowDefinition Height="50px"/>
        </Grid.RowDefinitions>
        <materialDesign:PackIcon Kind="FolderOpen" Grid.Row="0"
                                 VerticalAlignment="Stretch" 
                                 HorizontalAlignment="Center"
                                 Width="40" Height="40"
                                 x:Name="Icon"/>
        <AccessText Grid.Row="1" TextWrapping="WrapWithOverflow" 
                    MaxHeight="45" Text="txname" VerticalAlignment="Center"
                    HorizontalAlignment="Center" TextAlignment="Center"
                    x:Name="displayTitle"/>
    </Grid>
</Button>
