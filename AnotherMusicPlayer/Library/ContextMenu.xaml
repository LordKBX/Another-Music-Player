<ContextMenu x:Class="AnotherMusicPlayer.LibraryContextMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <!-- PARTIE BACK BUTTON -->
    <MenuItem  Name="BackFolder" Header="{DynamicResource LibraryContextMenuGetBack}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="ArrowLeftTop"/>
        </MenuItem.Icon>
    </MenuItem>

    <!-- ADD IN PLAYLIST ORDONED -->
    <MenuItem  Name="AddFolder" Header="{DynamicResource LibraryContextMenuAddFolder}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="PlaylistPlus"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Name="AddTrack" Header="{DynamicResource LibraryContextMenuAddTrack}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="PlaylistPlus"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Name="AddAlbum" Header="{DynamicResource LibraryContextMenuAddAlbum}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="PlaylistPlus"/>
        </MenuItem.Icon>
    </MenuItem>

    <!-- ADD IN PLAYLIST RANDOMIZED -->
    <MenuItem  Name="AddShuffleFolder" Header="{DynamicResource LibraryContextMenuAddShuffleFolder}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="ShuffleVariant"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Name="AddShuffleAlbum" Header="{DynamicResource LibraryContextMenuAddShuffleAlbum}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="ShuffleVariant"/>
        </MenuItem.Icon>
    </MenuItem>

    <!-- REPLACE PLAYLIST ORDONED -->
    <MenuItem  Name="PlayFolder" Header="{DynamicResource LibraryContextMenuPlayFolder}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="FolderPlay"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Name="PlayTrack" Header="{DynamicResource LibraryContextMenuPlayTrack}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="PlayBox"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Name="PlayAlbum" Header="{DynamicResource LibraryContextMenuPlayAlbum}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="Album"/>
        </MenuItem.Icon>
    </MenuItem>

    <!-- REPLACE PLAYLIST RANDOMIZED -->
    <MenuItem  Name="PlayShuffleFolder" Header="{DynamicResource LibraryContextMenuPlayShuffleFolder}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="BowlMix"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Name="PlayShuffleAlbum" Header="{DynamicResource LibraryContextMenuPlayShuffleAlbum}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="BowlMix"/>
        </MenuItem.Icon>
    </MenuItem>

    <!-- PARTIE Edition -->
    <MenuItem  Name="EditFile" Header="{DynamicResource LibraryContextMenuEditFile}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="FileEdit"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem  Name="EditTrack" Header="{DynamicResource LibraryContextMenuEditTrack}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="FileEdit"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Name="EditFolder" Header="{DynamicResource LibraryContextMenuEditFolder}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="FolderEdit"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Name="EditAlbum" Header="{DynamicResource LibraryContextMenuEditAlbum}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="FolderEdit"/>
        </MenuItem.Icon>
    </MenuItem>

    <!-- PARTIE ADD INTO A PLAYLIST -->
    <MenuItem  Name="PlayListsAddFolder" Header="{DynamicResource LibraryContextMenuPlayListsAddFolder}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="TableColumnPlusAfter"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Name="PlayListsAddTrack" Header="{DynamicResource LibraryContextMenuPlayListsAddTrack}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="TableColumnPlusAfter"/>
        </MenuItem.Icon>
    </MenuItem>
    <MenuItem Name="PlayListsAddAlbum" Header="{DynamicResource LibraryContextMenuPlayListsAddAlbum}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="TableColumnPlusAfter"/>
        </MenuItem.Icon>
    </MenuItem>
</ContextMenu>
