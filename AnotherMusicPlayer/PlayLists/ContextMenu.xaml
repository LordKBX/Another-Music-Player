<ContextMenu x:Class="AnotherMusicPlayer.PlayListsContextMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">

    <!-- Playlist Tree Item ContextMenu -->
    <MenuItem Name="PlayList" Header="{DynamicResource LibraryContextMenuPlayList}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="PlaylistPlay"/>
        </MenuItem.Icon>
    </MenuItem>

    <!-- Playlist Item ContextMenu -->
    <MenuItem Name="RemoveTrack" Header="{DynamicResource LibraryContextMenuPlayList}">
        <MenuItem.Icon>
            <materialDesign:PackIcon Kind="Album"/>
        </MenuItem.Icon>
    </MenuItem>
</ContextMenu>
